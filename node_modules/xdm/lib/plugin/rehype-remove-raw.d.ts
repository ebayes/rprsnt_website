/**
 * A tiny plugin that removes raw HTML.
 * This is needed if the format is `md` and `rehype-raw` was not used to parse
 * dangerous HTML into nodes.
 *
 * @returns {Transformer}
 */
export function rehypeRemoveRaw(): Transformer
export type Node = import('unist').Node
export type Visitor = import('unist-util-visit').Visitor<Node>
export type Transformer = import('unified').Transformer
